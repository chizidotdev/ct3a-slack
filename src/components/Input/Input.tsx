import React from "react";

const Input = () => {
  return (
    <div className="col-span-3 border-t bg-primary p-2">
      <input
        className="w-full bg-white py-2 px-4"
        type="text"
        placeholder="CSS Grid Layout Module"
      />
    </div>
  );
};

export default Input;
